import Button from './button';

const meta = {
  component: Button,
};

export default meta;

export const Compass = {
  args: {
    symbol: 'compass',
  }
};

export const Museum = {
  args: {
    symbol: "museum"
  }
};

export const Inventory = {
  args: {
    symbol: "inventory"
  }
};

export const Ghost = {
  args: {
    symbol: "ghost"
  }
};

export const Scan = {
  args: {
    symbol: "scan"
  }
};